PORT=5001
# Optional: add a free GitHub Personal Access Token for 5000 req/hr instead of 60 req/hr
# Generate one at https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=
